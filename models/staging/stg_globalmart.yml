version: 2 

models: 
  - name: stg_orders
    description: one unique order per row. 
    columns: 
      - name: orderid 
        description: the PK for stg_orders table. 
      - name: shipmode
        description: '{{doc("shipmode")}}'